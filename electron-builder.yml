appId: com.electron.app
productName: move-cursor-project
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
asarUnpack:
  - resources/**
win:
  executableName: move-cursor-project
  # Disable code signing for Windows - using proper configuration
  signAndEditExecutable: false
  # Disable certificate verification
  verifyUpdateCodeSignature: false
  target:
    - target: nsis
      arch:
        - x64
        - ia32
    - target: portable
      arch:
        - x64
        - ia32
nsis:
  artifactName: ${name}-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
  # Allow running without admin privileges
  oneClick: false
  allowToChangeInstallationDirectory: true
mac:
  # Disable code signing for macOS
  identity: null
  # Disable notarization
  notarize: false
  # Disable hardened runtime
  hardenedRuntime: false
  # Disable gatekeeper
  gatekeeperAssess: false
  target:
    - target: dmg
      arch:
        - x64
        - arm64
    - target: zip
      arch:
        - x64
        - arm64
dmg:
  artifactName: ${name}-${version}.${ext}
  # Disable code signing for DMG
  sign: false
linux:
  target:
    - AppImage
    - snap
    - deb
  maintainer: electronjs.org
  category: Utility
appImage:
  artifactName: ${name}-${version}.${ext}
npmRebuild: false
# Disable publishing for now
publish: null
